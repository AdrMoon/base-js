// const generateTitle = (text) => {
//   return `<h1>${text}</h1>`;
// };

// const generateListItem = (item) => {
//   return `<li>${item}</li>`;
// };

// export const generateSection = (section) => {
//   return `
//     <section>
//       ${generateTitle(section.title)}
//       <ul>
//         ${generateListItem(section.texts[0])}
//         ${generateListItem(section.texts[1])}
//       </ul>
//     </section>
//   `;
// };


